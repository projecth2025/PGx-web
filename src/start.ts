/**
 * Application start configuration.
 * The actual server/client entry points are in server.ts and client.ts.
 * API routes are handled via the custom Vite plugin in vite.config.ts.
 * Error handling for SSR is done in server.ts.
 */
